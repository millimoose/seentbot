# Load Pulumi ESC environment (millimoose/seentbot/dev)
# Requires: pulumi login, esc CLI
# Docs: https://www.pulumi.com/docs/esc/
eval "$(esc open millimoose/seentbot/dev --format shell)"

# Local overrides (only if not in ESC)
export DATABASE_URL=file:./dev.db
